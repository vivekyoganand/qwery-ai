apiVersion: v1
kind: Service
metadata:
  name: {{ include "qwery-ai.fullname" . }}
  labels:
    {{- include "qwery-ai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "qwery-ai.selectorLabels" . | nindent 4 }}
