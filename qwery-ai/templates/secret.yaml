apiVersion: v1
kind: Secret
metadata:
  name: {{ include "qwery-ai.fullname" . }}-secret
  labels:
    {{- include "qwery-ai.labels" . | nindent 4 }}
type: Opaque
stringData:
  postgres-password: "ChangeMe123!"  # This should match PostgreSQL password
  # Add other secrets as needed
  # api-key: "your-api-key"
